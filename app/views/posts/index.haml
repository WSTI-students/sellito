%h1
  Posts
- if current_user
  %p= button_to "Add post", new_post_path, method: :get, class: 'btn btn-lg btn-warning'
%p List of posts:
%ul
  - @posts.each do |post|
    %li
      = link_to post.title, post
      = link_to "Edit", edit_post_path(post) if post_owner_or_admin(post)
